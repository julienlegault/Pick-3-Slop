import React from 'react';
import ReactDOM from 'react-dom/client';

// TODO: import App from './App' once app.js is migrated to a module in Phase 2
function App() {
  return null;
}

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
